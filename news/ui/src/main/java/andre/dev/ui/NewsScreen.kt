package andre.dev.ui

import andre.dev.presentation.NewsViewModel
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun NewsScreen(viewModel: NewsViewModel) {

//    // Obtain the Factory from the provider
//    val factory = newsDependencyProvider.getViewModelFactory()
//
//    // Using LocalContext to obtain the FragmentActivity which is required for viewModel()
//    // This is just an example, ensure your context is correctly cast to FragmentActivity
//    val context = LocalContext.current
//
    Text("text")
}